name: Experiment
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Secret exposure test
      env:
        DEBUG_PASSWORD: ${{ secrets.DEBUG_PASSWORD }}
      run: echo $DEBUG_PASSWORD > secrets.txt

    - name: Run tmate
      uses: mxschmitt/action-tmate@v2


