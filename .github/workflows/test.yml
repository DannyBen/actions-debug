name: Experiment
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test that action

    steps:
    - uses: actions/checkout@v2
    - name: Debug
      run: |
        [[ ${{ secrets.TEST }} == there is no spoon ]] && echo "PASS" || echo "FAIL"

