name: Experiment
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test that action

    steps:
    - uses: actions/checkout@v2
    - env:
        BASE: Hello
        RESULT: "${{ env.BASE }} world"
      run: echo $RESULT

