name: Experiment
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test that action

    env:
      DOMAIN: example.com

    steps:
    - uses: actions/checkout@v2
    - env:
        URL: "${{ env.DOMAIN }}/page"
      run: echo $URL

